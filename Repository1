#include<stdio.h>
int main()
{
  float x, S;
  int i;
  S = 0;
  printf(" Uneti 5 brojeva:\n");
  for(i=1; i<=5; i=i+1);
  {
    printf("%d. ", i);
    scanf("%f", &x);
    S = S + x;
  }
  printf("Suma 5 brojeva iznosi %f", S);
}
